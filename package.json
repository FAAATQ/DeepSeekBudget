{
  "name": "deepseek-budget",
  "private": true,
  "version": "0.3.3",
  "description": "See the price. Know when to wait. A menu bar indicator for DeepSeek's peak/off-peak API pricing.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "icons": "tauri icon src-tauri/app-icon.png && python3 tools/optimize-icons.py",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
